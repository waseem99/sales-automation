name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - sprint-1-foundation

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          show-progress: false

      - name: Show runtime versions
        run: |
          node --version
          npm --version
          npm exec --yes --package=@pnpm/exe@9.15.9 -- pnpm --version

      - name: Install dependencies
        run: npm exec --yes --package=@pnpm/exe@9.15.9 -- pnpm install --no-frozen-lockfile --silent

      - name: Typecheck
        id: typecheck
        continue-on-error: true
        run: npm exec --yes --package=@pnpm/exe@9.15.9 -- pnpm typecheck > typecheck.log 2>&1

      - name: Print typecheck log tail
        if: always()
        run: tail -n 160 typecheck.log || true

      - name: Upload typecheck log
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: typecheck-log
          path: typecheck.log

      - name: Fail if typecheck failed
        if: steps.typecheck.outcome != 'success'
        run: exit 1

      - name: Test
        id: test
        continue-on-error: true
        run: npm exec --yes --package=@pnpm/exe@9.15.9 -- pnpm test > test.log 2>&1

      - name: Print test log tail
        if: always()
        run: tail -n 120 test.log || true

      - name: Upload test log
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: test-log
          path: test.log

      - name: Fail if tests failed
        if: steps.test.outcome != 'success'
        run: exit 1

      - name: Build
        run: npm exec --yes --package=@pnpm/exe@9.15.9 -- pnpm build
