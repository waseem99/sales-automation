name: Upwork Account Intelligence CI

on:
  pull_request:
    branches:
      - agent/offer-campaign-engine-230
    paths:
      - '.github/workflows/upwork-account-intelligence-ci.yml'
      - 'packages/upwork-account-intelligence/**'
      - 'packages/shared/**'
      - 'api/acquisition-ingest.ts'
      - 'vercel/upwork-account-intelligence-runtime.ts'
      - 'vercel/upwork-account-workspace-ui.ts'
      - 'vercel/workspace-dashboard-runtime.js'
      - 'vercel/prospect-workflow-ui.ts'
      - 'workers/acquisition/tests/upwork_account_intelligence_bridge_contract.mjs'
      - 'tsconfig.upwork-account-intelligence.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'packages/**'
      - 'apps/web/**'
      - 'vercel/**'
      - 'scripts/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: upwork-account-intelligence-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  behavior:
    name: Test account promotion and production integration
    runs-on: ubuntu-latest
    timeout-minutes: 28
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install standalone pnpm
        run: npm install --global --force @pnpm/exe@9.15.9

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Test Upwork account intelligence behavior
        id: behavior_tests
        continue-on-error: true
        shell: bash
        run: pnpm --filter @sales-automation/upwork-account-intelligence test > upwork-account-behavior.log 2>&1

      - name: Validate evidence-only account bridge
        id: bridge_contract
        if: steps.behavior_tests.outcome == 'success'
        continue-on-error: true
        working-directory: workers/acquisition
        run: node tests/upwork_account_intelligence_bridge_contract.mjs > ../../upwork-account-bridge.log 2>&1

      - name: Preserve existing Upwork extension contract
        id: extension_contract
        if: steps.behavior_tests.outcome == 'success' && steps.bridge_contract.outcome == 'success'
        continue-on-error: true
        working-directory: workers/acquisition
        run: node tests/upwork_extension_contract.mjs > ../../upwork-account-extension.log 2>&1

      - name: Build full production workspace
        id: production_build
        if: steps.behavior_tests.outcome == 'success' && steps.bridge_contract.outcome == 'success' && steps.extension_contract.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: pnpm build:vercel > upwork-account-production-build.log 2>&1

      - name: Preserve behavior diagnostics
        if: always()
        shell: bash
        run: |
          test -f upwork-account-behavior.log || touch upwork-account-behavior.log
          test -f upwork-account-bridge.log || touch upwork-account-bridge.log
          test -f upwork-account-extension.log || touch upwork-account-extension.log
          test -f upwork-account-production-build.log || touch upwork-account-production-build.log

      - name: Upload behavior diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: upwork-account-intelligence-behavior
          path: |
            upwork-account-behavior.log
            upwork-account-bridge.log
            upwork-account-extension.log
            upwork-account-production-build.log
          if-no-files-found: error
          retention-days: 3

      - name: Enforce account behavior tests
        if: steps.behavior_tests.outcome != 'success'
        run: |
          cat upwork-account-behavior.log
          exit 1

      - name: Enforce account bridge
        if: steps.behavior_tests.outcome == 'success' && steps.bridge_contract.outcome != 'success'
        run: |
          cat upwork-account-bridge.log
          exit 1

      - name: Enforce Upwork extension regression
        if: steps.bridge_contract.outcome == 'success' && steps.extension_contract.outcome != 'success'
        run: |
          cat upwork-account-extension.log
          exit 1

      - name: Enforce production build
        if: steps.extension_contract.outcome == 'success' && steps.production_build.outcome != 'success'
        run: |
          tail -n 360 upwork-account-production-build.log
          exit 1

  typecheck:
    name: Type-check Upwork account intake
    runs-on: ubuntu-latest
    timeout-minutes: 22
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install standalone pnpm
        run: npm install --global --force @pnpm/exe@9.15.9

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build all workspace packages
        id: workspace_build
        continue-on-error: true
        shell: bash
        run: pnpm build > upwork-account-workspace-build.log 2>&1

      - name: Type-check account-intelligence runtime
        id: focused_typecheck
        if: steps.workspace_build.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: pnpm exec tsc -p tsconfig.upwork-account-intelligence.json --noEmit > upwork-account-typecheck.log 2>&1

      - name: Preserve diagnostics
        if: always()
        shell: bash
        run: |
          test -f upwork-account-workspace-build.log || touch upwork-account-workspace-build.log
          test -f upwork-account-typecheck.log || touch upwork-account-typecheck.log

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: upwork-account-intelligence-typecheck
          path: |
            upwork-account-workspace-build.log
            upwork-account-typecheck.log
          if-no-files-found: error
          retention-days: 3

      - name: Enforce workspace build
        if: steps.workspace_build.outcome != 'success'
        run: |
          cat upwork-account-workspace-build.log
          exit 1

      - name: Enforce focused typecheck
        if: steps.workspace_build.outcome == 'success' && steps.focused_typecheck.outcome != 'success'
        run: |
          cat upwork-account-typecheck.log
          exit 1
