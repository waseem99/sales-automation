{
  "name": "@sales-automation/email-sources",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/index.test.ts && tsx src/gmail.test.ts",
    "ingest:gmail": "node dist/run-gmail.js"
  },
  "dependencies": {
    "@sales-automation/ingestion": "workspace:*",
    "@sales-automation/shared": "workspace:*",
    "@sales-automation/storage": "workspace:*"
  },
  "devDependencies": {
    "@sales-automation/fixtures": "workspace:*",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
