import fs from 'node:fs';

const pagePath = 'apps/web/src/prospects-page.ts';
const handlerPath = 'apps/web/src/prospect-handler.ts';
const workflowPath = '.github/workflows/apply-seller-queue-ui-patch.yml';
const scriptPath = 'scripts/apply-seller-queue-ui-patch.mjs';

let page = fs.readFileSync(pagePath, 'utf8');
const pageImportNeedle = "import type { StoredLeadRecord } from '@sales-automation/storage';\nimport { renderDecisionScorePanel } from './decision-score-view.js';";
const pageImportReplacement = "import type { SellerQueueView } from '@sales-automation/seller-queues';\nimport type { StoredLeadRecord } from '@sales-automation/storage';\nimport { renderDecisionScorePanel } from './decision-score-view.js';\nimport { renderSellerQueueNavigation } from './seller-queue-view.js';";
if (!page.includes(pageImportNeedle)) throw new Error('Prospect page import insertion point changed.');
page = page.replace(pageImportNeedle, pageImportReplacement);

const pageInterfaceNeedle = `export interface ProspectDashboardPageInput {\n  records: StoredLeadRecord[];\n  selected?: StoredLeadRecord;\n  runs: ProspectDiscoveryRun[];\n  generatedAt: string;\n}`;
const pageInterfaceReplacement = `export interface ProspectDashboardPageInput {\n  records: StoredLeadRecord[];\n  selected?: StoredLeadRecord;\n  runs: ProspectDiscoveryRun[];\n  generatedAt: string;\n  sellerQueue: SellerQueueView;\n  sellerUserId: string;\n}`;
if (!page.includes(pageInterfaceNeedle)) throw new Error('Prospect page input interface changed.');
page = page.replace(pageInterfaceNeedle, pageInterfaceReplacement);

const pageRenderNeedle = '</header>\n\n  <section class="metrics">';
const pageRenderReplacement = '</header>\n\n  ${renderSellerQueueNavigation(input.sellerQueue, input.sellerUserId)}\n\n  <section class="metrics">';
if (!page.includes(pageRenderNeedle)) throw new Error('Prospect page queue insertion point changed.');
page = page.replace(pageRenderNeedle, pageRenderReplacement);
fs.writeFileSync(pagePath, page, 'utf8');

let handler = fs.readFileSync(handlerPath, 'utf8');
const handlerImportNeedle = "import type { ProspectDiscoveryResult, ProspectDiscoveryRunStore } from '@sales-automation/prospect-discovery';";
const handlerImportReplacement = `import type { ProspectDiscoveryResult, ProspectDiscoveryRunStore } from '@sales-automation/prospect-discovery';\nimport {\n  buildSellerQueueView,\n  createSellerQueuePreferences,\n  decodeSellerQueuePreferenceCookie,\n  encodeSellerQueuePreferenceCookie,\n  readCookieValue,\n  SELLER_QUEUE_PREFERENCE_COOKIE,\n} from '@sales-automation/seller-queues';`;
if (!handler.includes(handlerImportNeedle)) throw new Error('Prospect handler import insertion point changed.');
handler = handler.replace(handlerImportNeedle, handlerImportReplacement);

const getBlockNeedle = `    if (method === 'GET' && (pathname === '/' || pathname === '/prospects')) {\n      const records = context.repository.listLeads();\n      const selectedId = url.searchParams.get('leadId') ?? undefined;\n      return html(renderProspectDashboardPage({\n        records,\n        selected: selectedId ? context.repository.getLead(selectedId) : records[0],\n        runs: context.runStore.listRuns(20),\n        generatedAt: now(context),\n      }));\n    }\n\n    if (method === 'GET' && pathname === '/api/prospects') {`;
const getBlockReplacement = `    if (method === 'GET' && (pathname === '/' || pathname === '/prospects')) {\n      const allRecords = context.repository.listLeads();\n      const savedCookie = readCookieValue(header(request.headers, 'cookie'), SELLER_QUEUE_PREFERENCE_COOKIE);\n      const saved = decodeSellerQueuePreferenceCookie(savedCookie, context.sessionSecret, actor);\n      const generatedAt = now(context);\n      const preferences = createSellerQueuePreferences({\n        userId: actor,\n        activeQueue: url.searchParams.get('queue') ?? saved?.activeQueue ?? 'follow_up_pending',\n        sort: url.searchParams.get('sort') ?? saved?.sort ?? 'priority_desc',\n        filters: {\n          serviceCategory: url.searchParams.has('service') ? url.searchParams.get('service') : saved?.filters.serviceCategory,\n          pipelineStatus: url.searchParams.has('status') ? url.searchParams.get('status') : saved?.filters.pipelineStatus,\n          owner: url.searchParams.has('owner') ? url.searchParams.get('owner') : saved?.filters.owner,\n          query: url.searchParams.has('q') ? url.searchParams.get('q') : saved?.filters.query,\n        },\n        savedAt: generatedAt,\n      });\n      const sellerQueue = buildSellerQueueView(allRecords, {\n        activeQueue: preferences.activeQueue,\n        sort: preferences.sort,\n        filters: preferences.filters,\n        generatedAt,\n      });\n      const selectedId = url.searchParams.get('leadId') ?? undefined;\n      const selected = selectedId\n        ? sellerQueue.records.find((record) => record.lead.id === selectedId)\n        : sellerQueue.records[0];\n      const response = html(renderProspectDashboardPage({\n        records: sellerQueue.records,\n        selected,\n        runs: context.runStore.listRuns(20),\n        generatedAt,\n        sellerQueue,\n        sellerUserId: actor,\n      }));\n      response.headers['set-cookie'] = \`${SELLER_QUEUE_PREFERENCE_COOKIE}=\${encodeURIComponent(encodeSellerQueuePreferenceCookie(preferences, context.sessionSecret))}; Path=/; HttpOnly; SameSite=Strict; Max-Age=2592000\${context.secureCookies ? '; Secure' : ''}\`;\n      return response;\n    }\n\n    if (method === 'GET' && pathname === '/api/seller-queues') {\n      const generatedAt = now(context);\n      const savedCookie = readCookieValue(header(request.headers, 'cookie'), SELLER_QUEUE_PREFERENCE_COOKIE);\n      const saved = decodeSellerQueuePreferenceCookie(savedCookie, context.sessionSecret, actor)\n        ?? createSellerQueuePreferences({userId: actor, activeQueue: 'follow_up_pending', sort: 'priority_desc', savedAt: generatedAt});\n      const sellerQueue = buildSellerQueueView(context.repository.listLeads(), {\n        activeQueue: saved.activeQueue,\n        sort: saved.sort,\n        filters: saved.filters,\n        generatedAt,\n      });\n      return json({\n        version: sellerQueue.version,\n        activeQueue: sellerQueue.activeQueue,\n        sort: sellerQueue.sort,\n        filters: sellerQueue.filters,\n        queues: sellerQueue.queues,\n        leadIds: sellerQueue.records.map((record) => record.lead.id),\n        countsReconciled: sellerQueue.countsReconciled,\n        humanReviewRequired: true,\n        externalActionAutomated: false,\n      });\n    }\n\n    if (method === 'POST' && pathname === '/api/seller-queue-preferences') {\n      const payload = asObject(request.body);\n      const preferences = createSellerQueuePreferences({\n        userId: actor,\n        activeQueue: payload.activeQueue,\n        sort: payload.sort,\n        filters: payload.filters,\n        savedAt: now(context),\n      });\n      const response = json({ok: true, preferences, externalActionAutomated: false}, 201);\n      response.headers['set-cookie'] = \`${SELLER_QUEUE_PREFERENCE_COOKIE}=\${encodeURIComponent(encodeSellerQueuePreferenceCookie(preferences, context.sessionSecret))}; Path=/; HttpOnly; SameSite=Strict; Max-Age=2592000\${context.secureCookies ? '; Secure' : ''}\`;\n      return response;\n    }\n\n    if (method === 'GET' && pathname === '/api/prospects') {`;
if (!handler.includes(getBlockNeedle)) throw new Error('Prospect handler queue insertion point changed.');
handler = handler.replace(getBlockNeedle, getBlockReplacement);
fs.writeFileSync(handlerPath, handler, 'utf8');

for (const path of [workflowPath, scriptPath]) if (fs.existsSync(path)) fs.unlinkSync(path);
console.log('Seller queue server filtering, signed preferences and Prospect Desk navigation integrated.');
